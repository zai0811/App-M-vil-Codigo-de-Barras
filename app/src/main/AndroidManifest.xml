<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application



        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"

    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/Base.AppTheme"
    tools:targetApi="31">


    <activity
        android:name=".LoginActivity"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity
        android:name=".ScanActivity"
        android:exported="true" />
    <activity android:name=".WelcomeActivity" android:exported="true" />
    <activity android:name=".Producto.ProductDetailsActivity" android:exported="true" />
        <activity
            android:name=".AllergySelectionActivity"
            android:exported="true"> <!-- Asegúrate de que está exportada correctamente -->
        </activity>
        <activity
            android:name=".AllergiesActivity"
            android:exported="true"/>

        <activity
            android:name=".RecipeActivity"
            android:exported="true"/>

        <activity
            android:name=".VideoPlayerActivity"
            android:exported="true"/>

        <activity
            android:name=".FavoritesActivity"
            android:exported="true" />    </application>
    </manifest>
